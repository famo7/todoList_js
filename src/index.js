import './style.css';
import { allProjects } from './modules/Controller';
// todos(title, description, dueDate, priority)
// projects(create new project, choose project todos go in)
//      Functionality:
// view all projects
// view all todos in each project(title, duedate), colors indicating priority
// expand single todo, see and edit
// delete todo
